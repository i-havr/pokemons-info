import { getHomePagePokemons } from 'services/PokemonApi';

// export const getPokemonsIdList = async limit => {
//   const result = (await getHomePagePokemons(limit)).results;
//   const idList = [];
//   result.map(item =>
//     idList.push({ name: item.name, id: item.url.slice(34, -1) })
//   );
//   // console.log(idList);
// };
